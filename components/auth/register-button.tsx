"use client";

import { Button } from "../ui/button";

export const RegisterButton = () => {
    return(
        <Button className="w-full" variant="default">
              Sign Up with Email
        </Button>
    );
}
