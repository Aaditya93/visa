"use client";

import { Button } from "../ui/button";

export const LoginButton = () => {
    return(
        <Button className="w-full" variant="default">
              Sign In
        </Button>
    );
}
